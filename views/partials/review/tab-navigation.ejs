<nav class="pd-tabs" aria-label="Product detail tabs">
    <% tabs.forEach((tab) => { %>
        <label
            class="pd-tab-btn"
            for="tab-<%= tab.id %>"
        >
            <%= tab.label %>
        </label>
    <% }); %>
</nav>