<section class="hero">
    <div class="hero__bg"></div>
    <div class="hero__title">
        <a class="hero__kicker" href="/cart/products">
            Step 1 of 3 · Your Bag
            <span class="hero__kicker-arrow">→</span>
        </a>
        <h1 class="script">Your Cart</h1>
        <span class="serif">Handpicked pieces, ready for checkout.</span>
    </div>
    <div class="hero__cta">
        <% if(cart.hasItems){ %>
            <a class="hero__cta-primary" href="/cart/checkout">Proceed to Checkout →</a>
            <% } %>
                <a class="hero__cta-ghost" href="/cart/products">Continue Shopping →</a>
    </div>
</section>